---
context: post-v1-multi-user-isolation
phase: post-v1
task: null
total_tasks: null
status: paused
last_updated: 2026-05-14T08:42:35.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: Account 2 login 500 — root cause UNKNOWN. auth.py now logs the exception. DO NOT guess fix. Check docker logs after fresh login attempt first.
- [ ] CONSTRAINT: auth.py has UNCOMMITTED change (try/except + logging block). Commit it before restarting container.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| database.py create-only init_db | init_db() with only CREATE TABLE IF NOT EXISTS silently fails on schema changes — existing tables never updated | blocking | Always add DROP+recreate migration when schema changes; check `sqlite_master` for column presence |
| Redirect to undefined route | /no-project not in React Router — *-catch-all fires immediately | blocking | Only redirect to routes defined in App.jsx (/, /dashboard, /bug-report, /sprint) |
| Diagnosing without logs | Guessing root cause of 500 without reading docker logs | blocking | Always: docker logs --tail 30 FIRST, then fix |
| git add -A or staging .planning/*.md | CRLF noise, unrelated files | advisory | Explicit file paths in git add |
| Edit .py without restarting container | uvicorn --reload blind to NTFS changes | advisory | Always docker restart after .py edit via powershell.exe |

## Infrastructure State

- **Git branch**: `main` (commits through 52db744)
- **Uncommitted**: `backend/routers/auth.py` — try/except + logging added to OAuth callback
- **Backend container**: Running — but has NOT been restarted with new auth.py code yet
- **Frontend container**: Running — no frontend changes this session
- **Docker in WSL**: CLI not available — always use `powershell.exe -Command "..."`
- **DB state**: All 4 data tables have `user_id INTEGER FK`, no `account_id` TEXT (confirmed via sqlite_master check)
- **DB users**: Only 1 user (ps@manaosoftware.com, user_id=1). Account 2 has never successfully logged in.

<current_state>
Account 2 login returns raw 500 Internal Server Error in browser. DB schema is confirmed correct (user_id present on all tables). The 500 errors visible in old docker logs were from SAME account as account 1 (pre-migration-fix failures). Root cause of account 2's 500 is UNKNOWN — it's different from the original oauth_tokens schema error.

This session added try/except + logger.error(traceback) around the DB/session block in auth.py callback. Once the container is restarted and account 2 tries again, the docker logs will show the exact exception.

Auth chain:
1. User clicks Login → /api/auth/atlassian → Atlassian OAuth
2. Callback: exchange code → fetch /me → upsert_user(atlassian_account_id) → user_id int
3. create_session(user_id) → session_id cookie (HttpOnly, SameSite=Lax, 7-day TTL)
4. New users → redirect to / (ConnectionPage); returning users → /dashboard
5. All API requests: Depends(get_current_user) reads session_id cookie → user_id int
</current_state>

<completed_work>

- `6b58216` — README Debugging Story rewrite
- `c8fcc83` — Multi-user data isolation (users/sessions tables, user_id FK everywhere)
- `71358eb` — DB migration (drop legacy account_id tables → recreate with user_id)
- `130497b` — Auth redirect fix (/ not /no-project)
- `UNCOMMITTED` — auth.py: try/except + logger.error(traceback.format_exc()) around DB/session block in OAuth callback
</completed_work>

<remaining_work>

- [ ] **FIRST**: Commit auth.py (`git add backend/routers/auth.py`)
- [ ] Restart backend: `powershell.exe -Command "docker restart group2_warnnoi300percent-backend-1"`
- [ ] Account 2 attempts login → should now get friendly error page (not raw 500)
- [ ] Immediately check logs: `powershell.exe -Command "docker logs group2_warnnoi300percent-backend-1 --tail 30 2>&1"`
- [ ] Find the `OAuth callback DB/session error:` line in logs → read traceback → fix root cause
- [ ] Verify both account 1 and account 2 can login successfully
- [ ] Verify sync — add Jira project → no "Sync failed"
- [ ] UAT: two browser profiles, different accounts → confirm isolated project lists
- [ ] UAT: Bug Report Word (.docx) and Sprint Word (.docx) exports
- [ ] Responsive UI polish UAT: sidebar animations, card layouts, navbar logo
</remaining_work>

<decisions_made>

- **users + sessions tables**: proper auth model — atlassian_account_id in users table, session_id cookie maps to user_id int
- **session_id cookie** (not jira_session): opaque token, safer
- **DB migration**: detect `account_id` in `sqlite_master` SQL → drop → recreate
- **Redirect new users to /**: /no-project doesn't exist in React Router
- **try/except in callback**: catches ALL DB/session exceptions, logs full traceback, returns redirect instead of 500
</decisions_made>

<blockers>

- Account 2 login 500 — root cause unknown. Next step: check docker logs after restart + fresh login attempt.
</blockers>

## Required Reading (in order)

1. `backend/routers/auth.py` — OAuth callback (lines ~98-170) — full flow with new try/except block
2. `backend/database.py` — init_db() migration section (lines ~102-165) — table drop+recreate logic
3. `backend/dependencies.py` — get_current_user dep (reads session_id cookie → user_id)
4. `backend/models/user.py` + `backend/models/session.py` — users/sessions table queries

## Critical Anti-Patterns (do NOT repeat)

- **Diagnose without logs**: Never guess root cause of 500. Run `docker logs --tail 30` first.
- **init_db() create-only**: `CREATE TABLE IF NOT EXISTS` never modifies existing tables. Schema changes ALWAYS need explicit migration.
- **Redirect to undefined route**: Check App.jsx routes before setting redirect target in auth.py.
- **git add .planning/ files**: CRLF noise — always use explicit paths.

<context>
DB schema correct. Old 500 errors were pre-migration-fix (same Atlassian account as account 1). Account 2 has never gotten in. Root cause of CURRENT 500 is unknown — likely something in the DB/session operations for a brand-new user. The try/except addition will surface the real exception in docker logs on next attempt.
</context>

<next_action>
1. Commit: `git add backend/routers/auth.py` then commit
2. Restart: `powershell.exe -Command "docker restart group2_warnnoi300percent-backend-1"`
3. Account 2 attempts login (browser will get friendly error page, not raw 500)
4. Immediately: `powershell.exe -Command "docker logs group2_warnnoi300percent-backend-1 --tail 30 2>&1"`
5. Look for `OAuth callback DB/session error:` line — read the traceback — fix root cause
</next_action>
