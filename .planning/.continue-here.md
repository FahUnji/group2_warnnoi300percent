---
context: post-v1-multi-user-isolation
phase: post-v1
task: null
total_tasks: null
status: paused
last_updated: 2026-05-14T08:52:34.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: Account 2 login root cause UNKNOWN. Step logging is active in backend. DO NOT guess or change code further. Run docker logs immediately after account 2 attempts login.
- [ ] CONSTRAINT: auth.py has UNCOMMITTED changes (+4 logging lines). Commit before any further edits.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| database.py create-only init_db | init_db() with CREATE TABLE IF NOT EXISTS silently ignores existing tables with wrong schema | blocking | Always add DROP+recreate migration; check sqlite_master for column presence |
| Redirect to undefined route | /no-project not in React Router — *-catch-all fires immediately | blocking | Only redirect to routes defined in App.jsx |
| Diagnose without logs | Changing code to fix a 500 without reading the actual traceback first | blocking | docker logs --tail 30 FIRST, then fix |
| git add -A or staging .planning/*.md | CRLF noise, unrelated files | advisory | Explicit file paths only |
| Edit .py without restarting container | uvicorn --reload works via WatchFiles inside container but blind to NTFS from outside WSL | advisory | docker restart OR let WatchFiles auto-reload (it works inside container) |

## Infrastructure State

- **Git branch**: `main` (last commit: 5a44b3d)
- **Uncommitted**: `backend/routers/auth.py` — +4 lines step logging (steps 1/2/3 + no account_id case)
- **Backend container**: Running — WatchFiles detected auth.py change and reloaded. New logging IS active.
- **Frontend container**: Running — no frontend changes this session
- **Docker in WSL**: CLI not available — always use `powershell.exe -Command "..."`
- **DB state**: All 4 data tables have `user_id INTEGER FK`, no `account_id` TEXT
- **DB users**: Only 1 user (ps@manaosoftware.com, user_id=1, projects: MLO + SAM). Account 2 never logged in.

<current_state>
Account 2 (same Atlassian org as account 1) still cannot login. They see "Authorization failed — please try again." on the ConnectionPage. This maps to the `token_exchange_failed` error code returned from auth.py.

auth.py now has logger.error() calls at EVERY failure point:
- "OAuth step 1 (token exchange) failed" — if POST to Atlassian fails
- "OAuth step 2 (/me fetch) failed" — if GET to api.atlassian.com/me fails
- "OAuth step 2 (/me) returned no account_id" — if /me response lacks account_id field
- "OAuth step 3 (accessible-resources) failed" — if GET accessible-resources fails
- "OAuth callback DB/session error" — if upsert_user / upsert_oauth_token / create_session fails

The backend has reloaded with this code (WatchFiles detected change). Account 2 has NOT yet attempted login since this reload.

Auth chain (when working):
1. User clicks Login → /api/auth/atlassian → Atlassian OAuth
2. Callback: exchange code → fetch /me → upsert_user(atlassian_account_id) → user_id int
3. create_session(user_id) → session_id cookie
4. New users → redirect to / (ConnectionPage); returning users → /dashboard
</current_state>

<completed_work>

- `6b58216` — README Debugging Story rewrite
- `c8fcc83` — Multi-user data isolation (users/sessions tables, user_id FK everywhere)
- `71358eb` — DB migration (drop legacy account_id tables → recreate with user_id)
- `130497b` — Auth redirect fix (/ not /no-project)
- `5a44b3d` — try/except + logging on DB/session block in OAuth callback
- `UNCOMMITTED` — auth.py: +4 lines logging on ALL error paths (step 1/2/3 + no account_id)
</completed_work>

<remaining_work>

- [ ] **FIRST**: Commit auth.py (`git add backend/routers/auth.py`)
- [ ] Account 2 attempts login at http://localhost:3000
- [ ] IMMEDIATELY check logs: `powershell.exe -Command "docker logs group2_warnnoi300percent-backend-1 --tail 15 2>&1"`
- [ ] Find the "OAuth step X" error line → fix that specific step
- [ ] Verify both accounts login successfully
- [ ] Verify sync — add Jira project → no "Sync failed"
- [ ] UAT: two browser profiles, different accounts → confirm isolated project lists
- [ ] UAT: Bug Report Word (.docx) and Sprint Word (.docx) exports
- [ ] Responsive UI polish UAT
</remaining_work>

<decisions_made>

- **users + sessions tables**: proper auth model with integer user_id FK
- **session_id cookie**: opaque token, safer than account_id in cookie
- **DB migration**: detect account_id in sqlite_master → drop → recreate
- **Redirect new users to /**: /no-project doesn't exist in React Router
- **Logging on ALL error paths**: Every OAuth callback exception now produces a named log line
</decisions_made>

<blockers>

- Account 2 login failure — root cause unknown. Step logging is now active. Needs fresh attempt + log check.
</blockers>

## Required Reading (in order)

1. `backend/routers/auth.py` — full OAuth callback (lines ~58-170) — all error paths now logged
2. `backend/database.py` — init_db() migration (lines ~102-165)
3. `backend/models/user.py` — upsert_user returns user_id int
4. `backend/models/oauth_token.py` — upsert_oauth_token uses user_id FK

## Critical Anti-Patterns (do NOT repeat)

- **Change code before reading logs**: Auth.py now logs every failure. Read the log first.
- **init_db() create-only**: Schema changes need explicit migration.
- **Redirect to undefined route**: Check App.jsx routes first.
- **git add .planning/ files**: CRLF noise.

<context>
Account 2 is from same Atlassian org — should work. The step logging will reveal exactly where the OAuth flow breaks. Most likely candidates: step 1 (token exchange network error or Atlassian returns non-200), step 2 (access_token invalid or /me response format differs), or DB step (upsert_user fails for new user). The DB schema is correct, so DB step failure would be surprising.
</context>

<next_action>
1. Commit: `git add backend/routers/auth.py` then commit
2. Account 2 attempts login at http://localhost:3000
3. IMMEDIATELY: `powershell.exe -Command "docker logs group2_warnnoi300percent-backend-1 --tail 15 2>&1"`
4. Read the "OAuth step X" error line
5. Fix that specific step
</next_action>
