---
context: phase
phase: 03-dashboard-ui
task: null
total_tasks: null
status: human_uat_pending
last_updated: 2026-05-12T10:40:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified. Advisory items only._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Spec-mockup mismatch | Original UI-SPEC was built from discuss-phase decisions (pie charts, Recharts) but the actual HTML mockup had no charts at all. Both CONTEXT.md and UI-SPEC.md were completely rewritten to match the mockup. | advisory | Always read UI/html/dashboard.html + UI/css/dashboard.css BEFORE writing any spec or plan. The mockup IS the source of truth. |
| Missing resp.ok check | All 7 fetch call-sites in DashboardPage.jsx call .json() without checking resp.ok — silent crashes on 4xx/5xx. Discovered in code review (CR-02). | advisory | Run /gsd-code-review 3 --fix before Phase 4. |
| URL injection in extractProjectKey | User input spliced into fetch URL with no allowlist validation. Discovered in code review (CR-01). | advisory | Fix CR-01 in 03-REVIEW.md before Phase 4 ships. |

<current_state>
Phase 3 execution complete. Both plans done and merged to dashboard-phase-3 branch.
- 03-01: DashboardPage.module.css (437 lines) + DashboardPage.jsx layout rewrite — committed
- 03-02: Data wiring complete — committed
- Code review: 3 critical, 7 warning, 3 info in 03-REVIEW.md
- Verification: human_needed (7/8 automated checks pass), 5 items in 03-HUMAN-UAT.md
- ROADMAP: Phase 3 = 2/2 plans done, Human UAT Pending
- Awaiting: human UAT approval → mark phase complete → fix blockers → Phase 4
</current_state>

<completed_work>

This session (2026-05-12):
- Wave 1 (03-01): DashboardPage.module.css created (437 lines, all CSS Modules classes); DashboardPage.jsx rewritten — bug table removed, PageHeader + ProjectsGrid + ConnectSection added
- Wave 2 (03-02): Real data wired — /api/projects on mount, parallel /api/bugs/{key} per card, handleConnect state machine (POST /api/sync/{key}), handleSync re-fetches stats
- Code review: 03-REVIEW.md (3 critical CR-01/02/03, 7 warning, 3 info)
- Verifier: 03-VERIFICATION.md (human_needed, 7/8 automated pass)
- 03-HUMAN-UAT.md created with 5 pending items
- HANDOFF.json + STATE.md + ROADMAP.md updated

Key commits:
- 1c05204 feat(03-01): create DashboardPage.module.css
- 9a950e7 feat(03-01): rewrite DashboardPage.jsx
- 60b09f6 feat(03-02): wire data fetching into DashboardPage
- 849a6de docs(03): code review report
- 9e9c035 test(03): human UAT items

Prior sessions:
- Phase 2 complete: JiraSyncService, NoProjectPage project picker, DashboardPage Sync UI, UAT 4/4
- Phase 3 pre-work: discuss-phase, UI-SPEC approved 6/6, CONTEXT.md D-01–D-09 locked
</completed_work>

<remaining_work>

- **Human UAT** (blocking for phase completion) — 5 items in 03-HUMAN-UAT.md:
  1. Build: `cd frontend && npm install && npm run build` — exit 0
  2. Visual: h1 green (#065b41), card grid, dark/white ConnectSection two-panel
  3. Card stats with live data: — → real OPEN BUGS + CRITICAL counts
  4. Bar color: red if critical > 0, green if critical = 0
  5. Connect E2E: key input → loading → success → card appears / invalid → error
- **Fix code review blockers** (recommended before Phase 4): `/gsd-code-review 3 --fix`
  - CR-01: URL injection in extractProjectKey — add `/^[A-Z][A-Z0-9]{1,9}$/` allowlist
  - CR-02: resp.ok never checked — add `if (!r.ok) throw` + Array.isArray guards
  - CR-03: setTimeout not cleared on unmount — useRef + cleanup
- **Mark phase complete**: ROADMAP Phase 3 → Complete, STATE → Phase 4 ready
</remaining_work>

<decisions_made>

Phase 3 (locked in 03-CONTEXT.md):
- D-01: No Recharts, no pie charts
- D-02: Per-card stats: OPEN BUGS (status open/'to do') + CRITICAL (priority critical/'highest') from GET /api/bugs/{key}
- D-03: Colored left bar: red (#dc2626) if critical > 0, green (#065b41) if critical = 0
- D-04: Hover animation translateY(-2px) + green shadow; sessionStorage click save
- D-05: Parallel per-project bug fetch on mount
- D-06: No separate summary cards row
- D-07: Remove bug table; keep Sync button + last-synced timestamp
- D-08: Add New Project connect section — POST /api/sync/{project_key}
- D-09: CSS Modules: DashboardPage.module.css (new file)

Prior phases:
- Phase 1: Atlassian OAuth 2.0 (3LO); Fernet-encrypted tokens in SQLite
- Phase 2: Bugs in SQLite; pagination via nextPageToken cursor; Bug/Bug Task only
</decisions_made>

<blockers>
- npm install fails on WSL NTFS (EACCES) — use Docker or native Linux
- backend/.env has secrets — gitignored, never commit
</blockers>

## Required Reading (in order)
1. `frontend/src/pages/DashboardPage.jsx` — current Phase 3 implementation
2. `frontend/src/pages/DashboardPage.module.css` — CSS Modules
3. `.planning/phases/03-dashboard-ui/03-HUMAN-UAT.md` — 5 UAT items pending
4. `.planning/phases/03-dashboard-ui/03-REVIEW.md` — CR-01/02/03 blockers
5. `UI/html/dashboard.html` + `UI/css/dashboard.css` — original mockup reference

## Infrastructure State
- Backend: FastAPI on :8000, SQLite at backend/jira_bugs.db, Python venv at backend/venv
- Frontend: React/Vite on :5173 (npm install required first — WSL EACCES workaround needed)
- Auth: Atlassian OAuth 2.0 (3LO); tokens in SQLite oauth_tokens table
- Branch: dashboard-phase-3

<context>
Phase 3 executed successfully. Two-wave execution: Wave 1 built static CSS + JSX structure, Wave 2 wired real data. Code review found 3 security/correctness blockers. Verifier requires human testing because npm install fails in WSL (NTFS permissions). All 7 automated must-haves pass.

Next session options:
A) Run UAT → approve → /gsd-code-review 3 --fix → plan Phase 4
B) Fix blockers first (/gsd-code-review 3 --fix) → UAT → mark complete → Phase 4
</context>

<next_action>
1. Start backend: `cd backend && source venv/bin/activate && uvicorn main:app --reload`
2. Start frontend: `cd frontend && npm run dev` (may need npm install first)
3. Test 5 items in 03-HUMAN-UAT.md
4. Reply "approved" or report issues
5. Then: /gsd-code-review 3 --fix
</next_action>
