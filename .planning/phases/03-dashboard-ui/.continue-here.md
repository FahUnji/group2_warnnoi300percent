---
context: phase
phase: 03-dashboard-ui
task: null
total_tasks: null
status: human_uat_pending
last_updated: 2026-05-12T16:10:58Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified. Advisory items only._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Spec-mockup mismatch | Original UI-SPEC was built from discuss-phase decisions (pie charts, Recharts) but the actual HTML mockup had no charts at all. Both CONTEXT.md and UI-SPEC.md were completely rewritten to match the mockup. | advisory | Always read UI/html/dashboard.html + UI/css/dashboard.css BEFORE writing any spec or plan. The mockup IS the source of truth. |
| Missing resp.ok check | All fetch call-sites in DashboardPage.jsx call .json() without checking resp.ok — silent crashes on 4xx/5xx. Discovered in code review (CR-02). | advisory | Run /gsd-code-review 3 --fix before Phase 4. |
| URL injection in extractProjectKey | User input spliced into fetch URL with no allowlist validation. Discovered in code review (CR-01). | advisory | Fix CR-01 in 03-REVIEW.md before Phase 4 ships. |

<current_state>
Phase 3 execution complete. All plans + post-plan additions committed. Human UAT is the only remaining gate.

Commits since last pause (8fdec32 is latest):
- 166c97c: project search in navbar + NoProjectPage privacy fix (GET /api/projects/search)
- 97fe240: search-and-select replaces Add Project form (PROJ-01)
- 37bfb5a: kebab menu with remove action (DELETE /api/projects/{key}); navigate /no-project when last removed (PROJ-03)
- 57046df: always-visible 3-dot button (removed opacity:0 hover-only)
- 69be528: planning docs corrected (OAuth 2.0, SQLite, search flow, PROJ-03 added)
- 8fdec32: critical count fixed — isNotDone predicate, only counts bugs not done AND critical/highest priority
- ab7388c: TOTAL BUGS stat added to project cards — unfiltered count, full-width above OPEN/CRITICAL row

Awaiting: human UAT approval → fix CR-01/02/03 → mark phase complete → Phase 4
</current_state>

<completed_work>

Session 1 (2026-05-12 morning):
- Wave 1 (03-01): DashboardPage.module.css created (437 lines); DashboardPage.jsx rewritten — bug table removed, PageHeader + ProjectsGrid + ConnectSection added
- Wave 2 (03-02): Real data wired — /api/projects on mount, parallel /api/bugs/{key} per card, handleConnect state machine, handleSync re-fetch
- Code review: 03-REVIEW.md (3 critical CR-01/02/03, 7 warning, 3 info)
- Verifier: 03-VERIFICATION.md (human_needed, 7/8 automated pass)
- 03-HUMAN-UAT.md created with 5 pending items

Session 2 (2026-05-12 afternoon):
- GET /api/projects/search + JiraSyncService.search_projects() — live Jira project search
- NoProjectPage: removed auto-load, switched to search-only (privacy-by-default fix)
- DashboardPage navbar: debounced project search dropdown (300ms)
- DELETE /api/projects/{project_key}: parameterized queries on bugs + jira_projects tables
- Kebab 3-dot menu on each project card (wrapper div + sibling button — avoids nested button HTML violation)
- onMouseDown stopPropagation prevents document listener from closing menu on same click that opens it
- Always-visible button (user preference — removed hover-only opacity:0)
- Planning docs corrected: OAuth 2.0, SQLite, search flow, PROJ-03 added to requirements
- Critical count fix: isNotDone predicate — critical = status != done AND priority critical/highest
- TOTAL BUGS stat: `total = bugs.length` (no filter), full-width `.statBox` above `.statBoxes` row; scoped `flex: 1` to `.statBoxes .statBox` to prevent vertical stretch
</completed_work>

<remaining_work>

- **Human UAT** (blocking for phase completion) — 5 items in 03-HUMAN-UAT.md:
  1. Build: `cd frontend && npm install && npm run build` — exit 0
  2. Visual: h1 green (#065b41), card grid, dark/white ConnectSection two-panel
  3. Card stats: loading state → real OPEN BUGS + CRITICAL counts (critical excludes resolved)
  4. Bar color: red if critical > 0, green if critical = 0
  5. Connect E2E: valid key → loading → success → card appears; invalid → error state
- **Fix code review blockers** (recommended before Phase 4): `/gsd-code-review 3 --fix`
  - CR-01: URL injection in extractProjectKey — add `/^[A-Z][A-Z0-9]{1,9}$/` allowlist
  - CR-02: resp.ok never checked — add `if (!r.ok) throw` + Array.isArray guards
  - CR-03: setTimeout not cleared on unmount — useRef + cleanup
- **Mark phase complete**: ROADMAP Phase 3 → Complete, STATE → Phase 4 ready
</remaining_work>

<decisions_made>

Phase 3 additions (this session):
- isNotDone predicate for bug stats — critical must exclude resolved bugs (prevents inflated counts)
- Kebab menu as sibling button in wrapper div — nested `<button>` is invalid HTML
- Always-visible 3-dot button — user preference, hover-only too hard to discover
- Privacy-by-default project search — no auto-load on mount; user types first
- DELETE /api/projects/{key} parameterized on both bugs + jira_projects tables
- navigate('/no-project', { replace: true }) when projects.length === 0 after delete
- TOTAL BUGS = `bugs.length` (no filter) — all synced bugs regardless of status/priority

Phase 3 locked decisions (03-CONTEXT.md D-01–D-09):
- D-01: No Recharts, no pie charts
- D-02: Per-card stats: OPEN BUGS + CRITICAL from GET /api/bugs/{key}
- D-03: Colored left bar: red (#dc2626) if critical > 0, green (#065b41) if critical = 0
- D-05: Parallel per-project bug fetch on mount
- D-09: CSS Modules: DashboardPage.module.css

Prior phases:
- Phase 1: Atlassian OAuth 2.0 (3LO); Fernet-encrypted tokens in SQLite
- Phase 2: Bugs in SQLite; pagination via nextPageToken cursor; Bug/Bug Task only
</decisions_made>

<blockers>
- npm install fails on WSL NTFS (EACCES) — use Docker or native Linux path
- backend/.env has secrets — gitignored, never commit
</blockers>

## Required Reading (in order)

1. `.planning/phases/03-dashboard-ui/03-HUMAN-UAT.md` — 5 UAT items, all must pass before phase close
2. `.planning/phases/03-dashboard-ui/03-REVIEW.md` — CR-01/02/03 to fix after UAT
3. `frontend/src/pages/DashboardPage.jsx` — current implementation
4. `UI/html/dashboard.html` + `UI/css/dashboard.css` — original mockup (source of truth)

## Infrastructure State

- Backend: FastAPI on :8000, SQLite at backend/jira_bugs.db, Python venv at backend/venv
- Frontend: React/Vite on :5173 (npm install required — WSL EACCES workaround needed)
- Auth: Atlassian OAuth 2.0 (3LO); tokens encrypted in SQLite oauth_tokens table
- All changes on main branch (no feature branch)

<context>
Phase 3 is complete from an implementation standpoint. Dashboard shows open + critical bug counts per project (critical excludes resolved bugs), project search works with privacy-by-default, users can remove projects via always-visible kebab menu. All committed clean. Human UAT is the only gate. After UAT, fix 3 code review blockers then close Phase 3 and begin Phase 4 (Sprint Report).
</context>

<next_action>
1. Start backend: `cd backend && source venv/bin/activate && uvicorn main:app --reload`
2. Start frontend: `cd frontend && npm run dev` (npm install first if not done)
3. Test 5 items in 03-HUMAN-UAT.md
4. Reply "approved" or report issues
5. Then: /gsd-code-review 3 --fix
</next_action>
