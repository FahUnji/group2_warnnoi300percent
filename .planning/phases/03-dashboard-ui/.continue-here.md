---
context: phase
phase: 03-dashboard-ui
task: null
total_tasks: null
status: human_uat_pending
last_updated: 2026-05-13T01:38:40Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified. Advisory items only._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Spec-mockup mismatch | Original UI-SPEC was built from discuss-phase decisions (pie charts, Recharts) but actual HTML mockup had no charts. Both CONTEXT.md and UI-SPEC.md completely rewritten to match mockup. | advisory | Always read UI/html/dashboard.html + UI/css/dashboard.css BEFORE writing any spec or plan. Mockup IS source of truth. |
| Missing resp.ok check | All fetch call-sites in DashboardPage.jsx call .json() without checking resp.ok — silent crashes on 4xx/5xx. Discovered in code review (CR-02). | advisory | Run /gsd-code-review 3 --fix before Phase 4. |
| URL injection in extractProjectKey | User input spliced into fetch URL with no allowlist validation. Discovered in code review (CR-01). | advisory | Fix CR-01 in 03-REVIEW.md before Phase 4 ships. |

<current_state>
Phase 3 UI is complete and polished. One uncommitted file: `frontend/src/pages/DashboardPage.module.css`.

This session (2026-05-13):
- Merged NoProjectPage into DashboardPage — single /dashboard route, 3-way ternary: loadingProjects → empty → has-projects (commit e95f67c)
- Added entry/exit animations: fadeInUp (page + cards staggered), cardExit, fadeOutUp (dashboard→empty transition), blink fix (commit 54bba42)
- Add-project search refactored to overlay dropdown — no layout shift (commit 5d2186c)
- UNCOMMITTED: topnav overflow:visible + z-index 100→1000 (fixes sticky nav stacking context)
- UNCOMMITTED: connectSection overflow:hidden removed; border-radius moved to child panels (fixes add-project dropdown clipping on with-projects page)

Next: commit the CSS fixes, then Human UAT.
</current_state>

<completed_work>

Session 1 (2026-05-12 morning):
- Wave 1 (03-01): DashboardPage.module.css created; DashboardPage.jsx rewritten
- Wave 2 (03-02): Real data wired — /api/projects, parallel /api/bugs/{key}, handleConnect, handleSync
- Code review: 03-REVIEW.md (CR-01/02/03 critical, 7 warning, 3 info)
- Verifier: 03-VERIFICATION.md (human_needed, 7/8 automated pass)

Session 2 (2026-05-12 afternoon/night):
- GET /api/projects/search + JiraSyncService.search_projects()
- NoProjectPage privacy fix (no auto-load on mount)
- Navbar debounced project search dropdown (300ms)
- DELETE /api/projects/{project_key} parameterized queries
- Kebab 3-dot menu on each project card
- Always-visible 3-dot button
- Planning docs corrected
- Critical count fix: isNotDone predicate
- TOTAL BUGS stat added to cards
- ShowAddDropdown overlay pattern, outside-click dismissal

Session 3 (2026-05-13):
- Merged NoProjectPage into DashboardPage (e95f67c)
- Entry/exit animations + blink fix (54bba42)
- Add-project overlay dropdown, no layout shift (5d2186c)
- UNCOMMITTED: topnav overflow/z-index fix + connectSection overflow:hidden removal
</completed_work>

<remaining_work>

- **COMMIT first**: `frontend/src/pages/DashboardPage.module.css` (dropdown clipping fixes)
- **Human UAT** (blocking for phase completion) — 5 items in 03-HUMAN-UAT.md:
  1. Build: `cd frontend && npm install && npm run build` — exit 0
  2. Visual: h1 green (#065b41), card grid, dark/white ConnectSection two-panel
  3. Card stats: loading state → real OPEN BUGS + CRITICAL counts (critical excludes resolved)
  4. Bar color: red if critical > 0, green if critical = 0
  5. Connect E2E: valid key → loading → success → card appears; invalid → error state
- **Fix code review blockers** (recommended before Phase 4): `/gsd-code-review 3 --fix`
  - CR-01: URL injection in extractProjectKey — add `/^[A-Z][A-Z0-9]{1,9}$/` allowlist
  - CR-02: resp.ok never checked — add `if (!r.ok) throw` + Array.isArray guards
  - CR-03: setTimeout not cleared on unmount — useRef + cleanup
- **Mark phase complete**: ROADMAP Phase 3 → Complete, STATE → Phase 4 ready
</remaining_work>

<decisions_made>

Session 3 additions:
- Single /dashboard route with 3-way ternary — eliminates flash-of-wrong-state on load
- Exit animation: isLeavingDashboard state + setTimeout cascade (240ms card exit, then 560ms state flip)
- connectSection overflow:hidden removed — was clipping absolute add-project dropdown; border-radius moved to child panels
- topnav z-index 100→1000 + overflow:visible — ensures sticky nav stacking context above animated content

Prior sessions (locked):
- isNotDone predicate for bug stats; kebab as sibling button; always-visible 3-dot; privacy-by-default search; TOTAL BUGS = bugs.length
- Phase 1: Atlassian OAuth 2.0 (3LO); Fernet-encrypted tokens in SQLite
- Phase 2: Bugs in SQLite; nextPageToken pagination; Bug/Bug Task only
</decisions_made>

<blockers>
- npm install fails on WSL NTFS (EACCES) — use Docker or native Linux path
- backend/.env has secrets — gitignored, never commit, stage only specific files
</blockers>

## Required Reading (in order)

1. `.planning/phases/03-dashboard-ui/03-HUMAN-UAT.md` — 5 UAT items, all must pass before phase close
2. `.planning/phases/03-dashboard-ui/03-REVIEW.md` — CR-01/02/03 to fix after UAT
3. `frontend/src/pages/DashboardPage.jsx` — current implementation
4. `UI/html/dashboard.html` + `UI/css/dashboard.css` — original mockup (source of truth)

## Infrastructure State

- Backend: FastAPI on :8000, SQLite at backend/jira_bugs.db, Python venv at backend/venv
- Frontend: React/Vite on :5173 (npm install required — WSL EACCES workaround needed)
- Auth: Atlassian OAuth 2.0 (3LO); tokens encrypted in SQLite oauth_tokens table
- All changes on main branch (no feature branch)

<context>
Phase 3 is complete and polished. Animations work, dropdowns don't clip, search UX is solid. One uncommitted file. Human UAT is the only gate. After UAT: fix 3 code review blockers (CR-01/02/03), mark phase complete, begin Phase 4 (Sprint Report).
</context>

<next_action>
1. Commit: `git add frontend/src/pages/DashboardPage.module.css && git commit -m "fix(ui): fix dropdown clipping — topnav overflow:visible + z-index 1000; connectSection overflow:hidden removed"`
2. Start backend: `cd backend && source venv/bin/activate && uvicorn main:app --reload`
3. Start frontend: `cd frontend && npm run dev`
4. Test 5 items in 03-HUMAN-UAT.md
5. Then: /gsd-code-review 3 --fix
</next_action>
