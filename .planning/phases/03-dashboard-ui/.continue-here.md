---
context: phase
phase: 03-dashboard-ui
task: null
total_tasks: null
status: ready_to_plan
last_updated: 2026-05-12T09:55:31Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Spec-mockup mismatch | Original UI-SPEC was built from discuss-phase decisions (pie charts, Recharts) but the actual HTML mockup had no charts at all. Both CONTEXT.md and UI-SPEC.md were completely rewritten to match the mockup. | advisory | Always read primary reference files (UI/html/dashboard.html, UI/css/dashboard.css) BEFORE writing any spec or plan for Phase 3. The mockup IS the source of truth. |
| Spacing non-multiples-of-4 | dashboard.css uses 10px, 14px, 3px — none are multiples of 4. UI-SPEC checker blocked on these. Resolved by rounding (10→8, 14→16, 3→4) and adding exceptions table entries for 20px, 40px, 12px, 56px, 160px. | advisory | When implementing from dashboard.css, use the UI-SPEC values (already rounded), NOT the raw CSS values. |

<current_state>
Phase 3 (dashboard-ui) planning not yet started. UI-SPEC approved (6/6 dimensions pass, reviewed 2026-05-12). CONTEXT.md fully rewritten to match dashboard.html mockup. No PLAN.md exists yet.
</current_state>

<completed_work>

Phase 2 complete:
- JiraSyncService — fetch bugs + upsert SQLite
- NoProjectPage project picker + DashboardPage Sync Now UI
- UAT 4/4 passed
- Subtask fetch removed (jira_sync_service.py committed as f45d9fc)

Phase 3 pre-work complete:
- discuss-phase 3: captured original decisions (Recharts, pie charts, card grid, remove bug table)
- ui-phase 3 iteration 1: initial UI-SPEC (based on discuss decisions) — later superseded
- **Mid-session redesign**: user confirmed Phase 3 should match UI/html/dashboard.html exactly
- CONTEXT.md rewritten: D-01 thru D-09 all updated (no charts, per-card stats, colored left bar, connect section, CSS Modules)
- UI-SPEC.md rewritten: typography 4 sizes / 2 weights, spacing exceptions table, card + connect section specs
- UI-SPEC checker: APPROVED 6/6 after 2 iterations
- Both files committed: 71a4e56 "docs(03): update UI-SPEC + CONTEXT to match dashboard.html mockup"
</completed_work>

<remaining_work>

- **plan-phase 3** — run `/gsd-plan-phase 3`; creates PLAN.md file(s) for Phase 3
- **execute-phase 3** — implement `DashboardPage.jsx` + `DashboardPage.module.css` per PLAN.md
</remaining_work>

<decisions_made>

Phase 3 design decisions (all in 03-CONTEXT.md):
- D-01: No Recharts, no pie charts
- D-02: Per-card stats: OPEN BUGS (status open/'to do') + CRITICAL (priority critical/'highest') — computed from GET /api/bugs/{project_key}
- D-03: Colored left bar: red (#dc2626) if critical > 0, green (#065b41) if critical = 0
- D-04: Hover animation (translateY -2px + green shadow); sessionStorage click save; no content change on click
- D-05: Parallel per-project bug fetch on mount
- D-06: No separate summary cards row
- D-07: Remove bug table; keep Sync button + last-synced timestamp
- D-08: Add New Project connect section — two-panel, POST /api/sync/{project_key}
- D-09: Create frontend/src/pages/DashboardPage.module.css (CSS Modules)
</decisions_made>

<blockers>
- backend/.env contains secrets — must NEVER be committed. File is gitignored; stage only specific files.
</blockers>

## Required Reading (in order)
1. `UI/html/dashboard.html` — PRIMARY reference; exact HTML structure to replicate in React
2. `UI/css/dashboard.css` — PRIMARY reference; exact CSS to replicate in CSS Modules
3. `.planning/phases/03-dashboard-ui/03-CONTEXT.md` — decisions D-01 thru D-09
4. `.planning/phases/03-dashboard-ui/03-UI-SPEC.md` — approved design contract (typography, colors, spacing exceptions, component specs)
5. `frontend/src/pages/DashboardPage.jsx` — existing Phase 2 implementation to modify

## Infrastructure State
- Backend: FastAPI on :8000, SQLite at backend/jira_bugs.db
- Frontend: React/Vite on :5173
- Auth: Atlassian OAuth 2.0 (3LO); tokens encrypted in SQLite
- Phase 2 bugs synced and accessible via GET /api/bugs/{project_key}

<context>
Phase 3 was redesigned mid-session. User asked "the ui is match After connect Jira -> Already Have Project.html?" — this revealed the original UI-SPEC (pie charts via Recharts) did NOT match the actual HTML mockup at UI/html/dashboard.html. User chose to implement the mockup exactly. CONTEXT.md and UI-SPEC.md were completely rewritten. UI-SPEC was approved by the checker after fixing spacing values (rounded non-multiples-of-4 from mockup CSS) and adding exceptions table entries.

The plan-phase 3 command was invoked at session end but interrupted before any planning work began.
</context>

<next_action>
Start with: `/gsd-plan-phase 3` — UI-SPEC approved, CONTEXT.md ready, no research needed.
Primary references: UI/html/dashboard.html + UI/css/dashboard.css
</next_action>
