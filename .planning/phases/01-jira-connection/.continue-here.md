---
context: phase
phase: 01-jira-connection
task: 0
total_tasks: 0
status: human_uat_pending
last_updated: 2026-05-11T09:49:05.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

_No blocking constraints. Previous advisory constraint (gsd-sdk not in PATH) documented below._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| gsd-sdk not in PATH | `gsd-sdk: command not found` on every SDK call. Entire Phase 1 workflow executed manually by reading .md files and constructing agent prompts from first principles. | advisory | Before any gsd-sdk call: `which gsd-sdk`. If missing, read workflow .md files directly and execute manually. |
| ConnectionForm uses absolute URLs | `frontend/src/components/ConnectionForm/ConnectionForm.jsx` hardcodes `http://localhost:8000/api/...`. Vite proxy in vite.config.js is dead code for the connect flow. | advisory | Works for local dev. If deploying, replace with relative `/api/...` URLs and let proxy handle it. |

<current_state>
Phase 1 "Jira Connection" fully implemented. All 3 waves executed (9 implementation commits). Code review done (01-REVIEW.md: 5 critical, 6 warning, 3 info). Automated verification done (01-VERIFICATION.md: 13/13 PASS). Human UAT defined (01-HUMAN-UAT.md: 5 tests pending browser execution).

User is paused to manually run the application and execute UAT tests. Nothing is blocked — the implementation is complete. Waiting for user to report "approved" or describe failures.
</current_state>

<completed_work>

All implementation (all commits on front-end-phase-1 branch):
- Wave 1 (PLAN-01): FastAPI scaffold, database.py, jira_config model, SQL migration, stub router
- Wave 2 (PLAN-02): JiraService (Fernet encryption + Jira Basic Auth), POST /api/jira/connect, GET /api/jira/status
- Wave 3 (PLAN-03): Vite+React scaffold, ConnectionForm, StatusBanner, SuccessModal, LoadingSpinner, routing

Planning artifacts:
- 01-REVIEW.md — code review (5 critical, 6 warning, 3 info)
- 01-VERIFICATION.md — automated verification (13/13 PASS, status: human_needed)
- 01-HUMAN-UAT.md — 5 UAT items defined (all pending browser execution)
</completed_work>

<remaining_work>

Human UAT (user executes in browser):
1. End-to-end success flow — valid credentials → spinner → SuccessModal → auto-redirect /dashboard
2. Invalid credentials error — wrong API token → StatusBanner with error message, api_token cleared
3. Unreachable host error — bad base_url → StatusBanner "Cannot reach Jira..."
4. App load auto-verify (no config) — fresh DB → ConnectionPage appears clean, no error banner
5. Responsive layout at 900px — hero left column hides below 900px, single-column layout

After UAT:
- "approved" → mark Phase 1 complete in ROADMAP.md + STATE.md, advance to Phase 2
- Issues reported → run /gsd-plan-phase 1 --gaps for gap closure

Optional (recommended before Phase 2):
- Fix 5 critical code review issues via /gsd-code-review 1 --fix
</remaining_work>

<decisions_made>

From 01-CONTEXT.md (D-01 through D-14) — all locked:
- D-01: 3 fields — base_url, email, api_token (no project key in Phase 1)
- D-03: Fernet encryption at rest, FERNET_KEY from .env
- D-04: FastAPI (async, Pydantic, auto-docs at /docs)
- D-07: separate dev servers — React :3000, FastAPI :8000, CORS locked to localhost:3000
- D-09: CSS Modules only, no Tailwind, no shadcn
- D-10: success → auto-redirect to /dashboard after 2000ms
- D-11: inline error, preserve base_url+email, clear api_token on error
- D-12: 3 error codes — invalid_credentials, unreachable_host, timeout
- D-13: error shape `{"ok": false, "error": "<code>", "message": "<text>"}` HTTP 400
- D-14: auto-verify on app load via GET /api/jira/status
</decisions_made>

<blockers>
None blocking execution. Human pre-checks to run the app:
1. MySQL running + migration executed (mysql -u root -p < migrations/001_create_jira_config.sql)
2. backend/.env configured (copy .env.example, set DB_PASSWORD, generate FERNET_KEY)
3. Backend started: cd backend && uvicorn main:app --reload --port 8000
4. Frontend started: cd frontend && npm install && npm run dev
</blockers>

## Required Reading (in order)

1. `.planning/phases/01-jira-connection/01-HUMAN-UAT.md` — 5 UAT tests to execute (browser testing)
2. `.planning/phases/01-jira-connection/01-REVIEW.md` — 5 critical issues found (CR-01 SSRF, CR-02 DB leak, CR-03 FERNET_KEY crash, CR-04 missing UNIQUE constraint, CR-05 unbounded token length)
3. `.planning/phases/01-jira-connection/01-VERIFICATION.md` — 13/13 automated must-haves PASS

## Infrastructure State

- Backend: implemented at `backend/`. Runs on :8000 via `uvicorn main:app --reload --port 8000`.
- Frontend: implemented at `frontend/`. Runs on :3000 via `npm run dev`.
- Database: migration SQL at `migrations/001_create_jira_config.sql`. Must be run once against MySQL.
- .env: `backend/.env.example` is the template. User must copy + fill DB_PASSWORD + generate FERNET_KEY.
- Dev servers: NOT running (user is paused). Must start both terminals manually.
- Code review findings: 5 critical issues exist but do NOT block UAT. They are advisories for Phase 2 gate.

<context>
Phase 1 is complete from a code standpoint. The UAT pause is intentional — user needs to manually set up the environment (MySQL, .env, both servers) and test the 5 browser scenarios. The main risk is the FERNET_KEY environment variable: if it's missing from .env, the backend will throw a KeyError on startup. This is one of the 5 critical code review findings (CR-03).

Critical issues from code review to keep in mind for Phase 2 planning:
- CR-01: SSRF — base_url validator accepts http:// (should be https-only + IP range check)
- CR-02: DB connection leak — no try/finally in jira_config model functions
- CR-03: FERNET_KEY crash — os.environ["FERNET_KEY"] raises KeyError if unset (use startup validation)
- CR-04: Missing UNIQUE constraint — jira_config table accumulates rows instead of updating
- CR-05: Unbounded api_token — no max length validator on Pydantic model
</context>

<next_action>
Wait for user UAT results.
- "approved" → update ROADMAP.md Phase 1 `[ ]` → `[x]`, update STATE.md to Phase 2, update REQUIREMENTS.md traceability for JIRA-01/02/03, present `/gsd-discuss-phase 2`.
- Issues reported → `/gsd-plan-phase 1 --gaps` for gap closure.
- Optional after approval: `/gsd-code-review 1 --fix` to remediate 5 critical findings.
</next_action>
