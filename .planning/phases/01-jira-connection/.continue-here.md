---
context: phase
phase: 01-jira-connection
task: verify-ui
status: paused
last_updated: 2026-05-11T10:58:12Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: UI scope changed — 3-field credential form removed. Original UAT tests (01-HUMAN-UAT.md) are OBSOLETE. Do NOT run them as-is.
- [ ] CONSTRAINT: WSL2+Docker HMR requires Vite polling — already in vite.config.js. Do not remove it.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stale UAT tests | UAT defined for 3-field form; form was removed | blocking | Re-scope UAT before running any tests |
| Browser cache after Docker rebuild | Old design persists after rebuild | advisory | Always Ctrl+Shift+R after docker compose up --build |

<current_state>

Docker fully rebuilt and running (from-scratch rebuild this session):
- Frontend: http://localhost:3000 (Vite dev server)
- Backend: http://localhost:8000 (FastAPI + SQLite)

ConnectionPage redesigned to match Figma (UI/html/login.html):
- REMOVED: 3-field form (base_url, email, api_token)
- ADDED: single "Continue with Atlassian" button → navigates to /dashboard
- SIMPLIFIED: App.jsx no longer calls /api/jira/status on startup

User paused before confirming new UI is visible at http://localhost:3000.
</current_state>

<completed_work>

- Wave 1–3: FastAPI backend + React frontend (all plans, committed)
- SQLite migration + Docker Compose setup (commit c31427c)
- All 11 CR/WR code review issues fixed (commit 2da6ea0)
- ConnectionPage redesigned to match Figma (commit 45aacf0)
- Vite usePolling added for WSL2+Docker HMR (commit 45aacf0)
- Docker rebuilt from scratch — both containers running
</completed_work>

<remaining_work>

1. Open http://localhost:3000 + Ctrl+Shift+R → confirm new Figma design visible
2. Re-scope UAT (new: page renders, button navigates to /dashboard, responsive at 900px)
3. Phase 1 approval (update ROADMAP.md + STATE.md)
4. /gsd-discuss-phase 2
</remaining_work>

<decisions_made>

- Removed 3-field form → single "Continue with Atlassian" button (user: match Figma UI/html/login.html)
- Button navigates directly to /dashboard — no real Atlassian OAuth
- App.jsx startup verify removed — dead code once credential form removed
- Vite usePolling:true (500ms) for WSL2+Docker file watching
- SQLite instead of MySQL — simpler UAT setup
- Docker Compose for one-command startup
</decisions_made>

<blockers>

- User has not confirmed new UI renders at http://localhost:3000
- Original UAT tests test the removed form — must re-scope before running
</blockers>

## Required Reading (in order)

1. `UI/html/login.html` — Figma reference the ConnectionPage was built to match
2. `frontend/src/pages/ConnectionPage.jsx` — new single-button implementation
3. `frontend/src/pages/ConnectionPage.module.css` — matching styles

## Infrastructure State

- **Docker**: both containers running. `docker compose up` to restart if stopped.
- **Frontend**: http://localhost:3000
- **Backend**: http://localhost:8000
- **DB**: SQLite at `backend/jira.db` — auto-created on backend start
- **Env**: `backend/.env` must contain FERNET_KEY

## Startup (if containers stopped)

```powershell
docker compose up
```

<context>

User redesigned login page mid-session to match Figma. Docker rebuilt from scratch. User paused before checking if new UI is showing. Next session: confirm UI, re-scope UAT, approve phase, move to Phase 2.

</context>

<next_action>

Start with: Open http://localhost:3000 + hard-refresh (Ctrl+Shift+R). Confirm single "Continue with Atlassian" button visible (no form fields). Then re-scope UAT and approve Phase 1.

</next_action>
