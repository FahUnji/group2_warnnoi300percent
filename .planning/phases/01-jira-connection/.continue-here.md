---
context: phase
phase: 01-jira-connection
task: verify-docker
status: paused
last_updated: 2026-05-11T10:45:17Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: UI scope changed — 3-field credential form removed. Original UAT tests (01-HUMAN-UAT.md) are OBSOLETE. Do NOT run them as-is.
- [ ] CONSTRAINT: WSL2+Docker HMR requires Vite polling — already added to vite.config.js. Do not remove it.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stale UAT tests | UAT defined for 3-field form; form was removed this session | blocking | Re-scope UAT before running any tests |
| Browser cache after Docker rebuild | Old design persists after rebuild | advisory | Always Ctrl+Shift+R after docker compose up --build |
| gsd-sdk not in PATH | gsd-sdk: command not found on every SDK call | advisory | Before any gsd-sdk call: `which gsd-sdk`. If missing, read workflow .md files directly. |

<current_state>
Phase 1 (Jira Connection) is implemented. This session redesigned ConnectionPage to match the Figma reference (UI/html/login.html):
- REMOVED: 3-field form (base_url, email, api_token)
- ADDED: single "Continue with Atlassian" button → navigates to /dashboard
- SIMPLIFIED: App.jsx no longer calls /api/jira/status on startup (dead code removed)
- ADDED: Vite usePolling:true (500ms) for WSL2+Docker HMR

Docker was running `docker compose up --build frontend` when paused. Build may still be in progress.

4 uncommitted source files: frontend/src/App.jsx, frontend/src/pages/ConnectionPage.jsx, frontend/src/pages/ConnectionPage.module.css, frontend/vite.config.js
</current_state>

<completed_work>

- Wave 1–3: FastAPI backend + React frontend (all plans executed, committed)
- SQLite migration (MySQL removed, auto-init on startup)
- Docker Compose setup (docker compose up = one-command start)
- All 11 CR/WR code review issues fixed (commit 2da6ea0)
- ConnectionPage redesigned to match Figma login.html — UNCOMMITTED
- Vite polling added for WSL2+Docker — UNCOMMITTED
</completed_work>

<remaining_work>

1. Check Docker build finished → verify http://localhost:3000 shows new design
2. Commit 4 uncommitted frontend files
3. Re-scope UAT (new tests: page renders, button navigates, responsive 900px)
4. Phase 1 approval (update ROADMAP.md + STATE.md)
5. /gsd-discuss-phase 2
</remaining_work>

<decisions_made>

- Removed 3-field form → single "Continue with Atlassian" button (user instruction to match Figma UI/html/login.html)
- Button navigates directly to /dashboard — no real Atlassian OAuth (UI-only change)
- App.jsx startup verify removed — credential storage flow gone, verify was dead code
- Vite usePolling:true (500ms) for WSL2+Docker file watching
- SQLite instead of MySQL (simpler UAT — no DB server needed)
- Docker Compose for dev environment (one-command startup)
- CSS Modules only, no Tailwind, no shadcn (hand-port from UI/html references)
</decisions_made>

<blockers>

- Docker rebuild may still be in progress — check terminal for "VITE ready"
- Original UAT tests (01-HUMAN-UAT.md) test removed credential form — must re-scope before running
</blockers>

## Required Reading (in order)

1. `UI/html/login.html` — Figma reference the ConnectionPage was built to match
2. `frontend/src/pages/ConnectionPage.jsx` — new single-button implementation
3. `frontend/src/pages/ConnectionPage.module.css` — matching styles
4. `.planning/HANDOFF.json` — full structured state

## Infrastructure State

- **Docker**: `docker compose up` starts everything. Frontend on :3000, backend on :8000.
- **DB**: SQLite at `backend/jira.db` — auto-created on first backend start
- **Env**: `backend/.env` must contain FERNET_KEY (generate: `python -c "from cryptography.fernet import Fernet; print('FERNET_KEY=' + Fernet.generate_key().decode())"`)
- **Backend auth API**: POST /api/jira/connect + GET /api/jira/status still exist but frontend no longer calls them

## Startup (if containers stopped)

```powershell
# From C:\Users\vs\group2_warnnoi300percent
docker compose up
```

Then open http://localhost:3000

<context>
User requested UI redesign mid-session to match Figma reference. The credential form (3 fields) is gone. Backend auth code still exists but is now unused by the frontend. The button click simply navigates to /dashboard. This is a significant scope change from the original Phase 1 plan — the phase still counts as done since the UI goal is met, but UAT tests need updating to reflect the new flow.
</context>

<next_action>
Start with: Check if `docker compose up --build frontend` finished (look for "VITE ready" in terminal). If yes → hard-refresh http://localhost:3000 (Ctrl+Shift+R) and verify new design. Then commit the 4 uncommitted files and re-scope UAT.
</next_action>
