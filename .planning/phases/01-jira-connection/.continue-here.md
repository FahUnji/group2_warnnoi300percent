---
context: phase
phase: 01-jira-connection
task: verify-and-close
status: paused
last_updated: 2026-05-11T13:30:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Original UAT tests (`01-HUMAN-UAT.md`) test the 3-field Basic Auth modal that was REMOVED. Do NOT run them as-is. Re-scope to 3 new OAuth tests before any UAT.
- [ ] CONSTRAINT: `backend/.env` holds real Atlassian OAuth credentials (gitignored). If containers restarted, env vars still load via `env_file`. Use `docker compose up -d --force-recreate backend` (not `restart`) to reload env changes.
- [ ] CONSTRAINT: OAuth scope `read:me` must be active in Atlassian developer console — "User Identity API" permission required. Already confirmed added by user.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stale UAT tests | UAT defined for old 3-field Basic Auth modal; replaced with OAuth button | blocking | Re-scope UAT before running any tests |
| `docker compose restart` for env changes | `restart` does NOT re-read `env_file` | blocking | Always `docker compose up -d --force-recreate backend` |
| `read:me` scope missing | `/api/auth/me` returns 403 "Insufficient scope" | blocking | Ensure "User Identity API" permission in Atlassian developer console |
| Browser cache after Docker rebuild | Old design persists | advisory | Always Ctrl+Shift+R after docker compose up --build |

<current_state>

Phase 1 complete. Auth switched from Basic Auth modal → Atlassian OAuth 2.0 (3LO).

**Authentication flow (CONFIRMED WORKING):**
- ConnectionPage: "Continue with Atlassian" button → `GET /api/auth/atlassian` → Atlassian login → callback → token stored (Fernet-encrypted in SQLite) → redirect to `/no-project` or `/dashboard` based on `count_projects()`
- No modal, no API token form — pure OAuth

**`/api/auth/me` endpoint (CONFIRMED WORKING):**
- Decrypts stored access_token, calls `https://api.atlassian.com/me`
- Returns `{"ok": true, "user": {"name": "...", "email": "...", "avatar": "..."}}`
- Confirmed real data: name="Kritimaporn (Grace) Kaweevoralux"

**Frontend pages using `/api/auth/me`:**
- `NoProjectPage.jsx` — reads sessionStorage first, falls back to `/api/auth/me`; shows user avatar + name in top-right nav
- `DashboardPage.jsx` — same pattern; shows user card (avatar + name + email + "Connected" badge)

**Backend:**
- `backend/routers/auth.py` — OAuth callback, CSRF state validation, token exchange, `/api/auth/me`
- `backend/models/oauth_token.py` — `upsert_oauth_token`, `load_oauth_token`
- `backend/.env` — real credentials (GITIGNORED):
  - `ATLASSIAN_CLIENT_ID=uElbZ6w6GNAObZDMYlGVqaeWB98pgWFd`
  - `ATLASSIAN_REDIRECT_URI=http://localhost:8000/api/auth/atlassian/callback`
  - `FRONTEND_URL=http://localhost:3000`

**User has tested and confirmed OAuth works end-to-end.**
</current_state>

<completed_work>

- Wave 1–3: FastAPI backend + React frontend (all plans, committed)
- SQLite migration + Docker Compose setup
- All CR/WR code review issues fixed
- ConnectionPage redesigned to match Figma (single button, hero grid layout)
- Footer pushed to bottom of screen
- NoProjectPage.jsx + NoProjectPage.module.css (matches Figma)
- jira_projects table + count_projects() in database.py
- **This session:** Replaced Basic Auth modal with Atlassian OAuth 2.0 (3LO)
- **This session:** `/api/auth/me` endpoint — decrypts token, calls Atlassian, returns user info
- **This session:** `read:me` scope added to OAuth scopes
- **This session:** `_decrypt_token` import fix in auth.py (was causing silent NameError)
- **This session:** ConnectionPage — OAuth button + error handling from `?error=` URL param
- **This session:** `.oauthError` CSS class in ConnectionPage.module.css
- **This session:** NoProjectPage + DashboardPage updated to use `/api/auth/me`
- **This session:** OAuth confirmed working with real Atlassian account
- **This session:** User info (name + avatar) confirmed displaying in NavBar
</completed_work>

<remaining_work>

1. Re-scope UAT — 3 new tests for OAuth flow:
   - ConnectionPage renders "Continue with Atlassian" button (no modal)
   - Button redirects to Atlassian OAuth (not a fetch call)
   - After OAuth → /no-project shows user name + avatar in navbar
2. Run re-scoped UAT
3. Approve Phase 1 (update ROADMAP.md + STATE.md)
4. `/gsd-discuss-phase 2`
</remaining_work>

<decisions_made>

- Atlassian OAuth 2.0 (3LO) over Basic Auth modal — required for future API access + better UX
- `count_projects()` in callback determines routing — no extra frontend API call
- Token encrypted with Fernet at rest in SQLite `oauth_tokens` table
- User info fetched from `https://api.atlassian.com/me` (requires `read:me` scope)
- sessionStorage for user state — cleared on tab close, re-fetched from `/api/auth/me` on next visit
- OAuth credentials in `backend/.env` (gitignored) — never committed
</decisions_made>

<blockers>

- UAT tests obsolete (test old Basic Auth modal) — must re-scope before running
</blockers>

## Required Reading (in order)

1. `backend/routers/auth.py` — full OAuth flow + `/api/auth/me` endpoint
2. `frontend/src/pages/ConnectionPage.jsx` — OAuth button + error handling
3. `frontend/src/pages/NoProjectPage.jsx` — user data from `/api/auth/me` + nav display
4. `frontend/src/pages/DashboardPage.jsx` — user card display

## Infrastructure State

- **Docker**: both containers running
- **Frontend**: http://localhost:3000 (Vite dev server, HMR polling 500ms)
- **Backend**: http://localhost:8000 (uvicorn --reload)
- **DB**: SQLite at `backend/jira.db` — `oauth_tokens` table holds encrypted token
- **Env**: `backend/.env` has real OAuth credentials + FERNET_KEY (gitignored)

## Startup (if containers stopped)

```bash
docker compose up -d --force-recreate
```

<context>

This session replaced Basic Auth modal with full Atlassian OAuth 2.0 (3LO). Flow confirmed working: OAuth button → Atlassian login → token stored (Fernet-encrypted) → `/no-project` page → user avatar + name visible in navbar. Only remaining work is UAT re-scope + phase close before Phase 2.
</context>

<next_action>

Re-scope `01-HUMAN-UAT.md` with 3 new tests for OAuth flow, run them, then `/gsd-discuss-phase 2`.
</next_action>
