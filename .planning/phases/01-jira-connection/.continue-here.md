---
context: phase
phase: 01-jira-connection
task: verify-and-close
status: paused
last_updated: 2026-05-12T03:29:49Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Atlassian OAuth app is in **Development mode** — user must add their Atlassian email in Distribution tab before OAuth login works. No code fix needed; this is a console setting.
- [ ] CONSTRAINT: Original UAT tests (`01-HUMAN-UAT.md`) test the 3-field Basic Auth modal that was REMOVED. Do NOT run them as-is. Re-scope to 3 new OAuth tests before any UAT.
- [ ] CONSTRAINT: `backend/.env` holds real Atlassian OAuth credentials (gitignored). If containers restarted, env vars still load via `env_file`. Use `docker compose up -d --force-recreate backend` (not `restart`) to reload env changes.
- [ ] CONSTRAINT: OAuth scope `read:me` must be active in Atlassian developer console — "User Identity API" permission required. Already confirmed added by user.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stale UAT tests | UAT defined for old 3-field Basic Auth modal; replaced with OAuth button | blocking | Re-scope UAT before running any tests |
| `docker compose restart` for env changes | `restart` does NOT re-read `env_file` | blocking | Always `docker compose up -d --force-recreate backend` |
| `read:me` scope missing | `/api/auth/me` returns 403 "Insufficient scope" | blocking | Ensure "User Identity API" permission in Atlassian developer console |
| Browser cache after Docker rebuild | Old design persists | advisory | Always Ctrl+Shift+R after docker compose up --build |

<current_state>

Phase 1 feature-complete. Auth uses Atlassian OAuth 2.0 (3LO).

**This session:** OAuth credentials (`ATLASSIAN_CLIENT_ID` + `ATLASSIAN_CLIENT_SECRET`) confirmed present in `backend/.env`. Backend container force-recreated — env vars verified loaded. User attempted login and got: **"This application is in development — only the owner of this application may grant it access to their account."**

This is an Atlassian console setting, not a code bug. Fix:
1. Go to https://developer.atlassian.com/console/myapps/
2. Open the OAuth app
3. **Distribution** tab → **Testing** section → add your Atlassian email as allowed user

Once that's done, OAuth login should complete end-to-end with no further code changes.

**Authentication flow (CONFIRMED IMPLEMENTED):**
- ConnectionPage: on mount → calls `/api/auth/me` → if token valid → redirect to `/no-project` or `/dashboard` (no login flash)
- Spinner shown during auth check (prevents ConnectionPage flash before redirect)
- "Continue with Atlassian" button → `GET /api/auth/atlassian` → Atlassian login → callback → token stored (Fernet-encrypted in SQLite) → redirect to `/no-project` or `/dashboard`
- Session persists across browser close/reopen (token in SQLite survives)

**Logout flow (IMPLEMENTED):**
- `POST /api/auth/logout` → `DELETE FROM oauth_tokens` → clears DB token
- Frontend: clears sessionStorage → redirects to `/`

**`/api/auth/me` returns:**
- `{"ok": true, "has_projects": false, "user": {"name": "...", "email": "...", "avatar": "..."}}`

**Frontend pages:**
- `ConnectionPage.jsx` — auth check on mount, spinner, OAuth button, error display from `?error=` param
- `NoProjectPage.jsx` — navbar with user avatar + name + logout dropdown (click-outside-to-close)
- `DashboardPage.jsx` — user card with avatar + name + email + inline "Sign out" button

**Backend:**
- `backend/routers/auth.py` — OAuth flow, CSRF state validation, token exchange, `/api/auth/me`, `POST /api/auth/logout`
- `backend/models/oauth_token.py` — `upsert_oauth_token`, `load_oauth_token`, `delete_oauth_token`
- `backend/.env` — real credentials (GITIGNORED)
</current_state>

<completed_work>

- Wave 1–3: FastAPI backend + React frontend (all plans, committed)
- SQLite migration + Docker Compose setup
- All CR/WR code review issues fixed
- ConnectionPage redesigned to match Figma (single button, hero grid layout)
- Footer pushed to bottom of screen
- NoProjectPage.jsx + NoProjectPage.module.css (matches Figma)
- jira_projects table + count_projects() in database.py
- Replaced Basic Auth modal with Atlassian OAuth 2.0 (3LO)
- `/api/auth/me` endpoint — decrypts token, calls Atlassian, returns user info + has_projects
- `read:me` scope + "User Identity API" permission added
- ConnectionPage OAuth button + error handling
- NoProjectPage + DashboardPage updated to show user info
- `delete_oauth_token()` in `backend/models/oauth_token.py`
- `POST /api/auth/logout` endpoint in `backend/routers/auth.py`
- `has_projects` field added to `/api/auth/me` response
- Logout button in NoProjectPage.jsx (nav dropdown) + DashboardPage.jsx (inline)
- Session persistence — ConnectionPage calls `/api/auth/me` on mount, auto-redirects if token valid
- Spinner on ConnectionPage during auth check (prevents login page flash)
- **This session:** OAuth credentials confirmed in `.env`, backend force-recreated, env vars verified loaded
</completed_work>

<remaining_work>

1. **[HUMAN ACTION REQUIRED]** Add test email in Atlassian developer console Distribution tab — unblocks OAuth login
2. Re-scope UAT (`01-HUMAN-UAT.md`) — 3 new tests for OAuth flow:
   - ConnectionPage renders "Continue with Atlassian" button (no modal, no Basic Auth)
   - Button redirects to Atlassian OAuth authorization URL (not a fetch call)
   - After OAuth → /no-project shows user name + avatar in navbar
3. Run re-scoped UAT
4. Approve Phase 1 (update ROADMAP.md + STATE.md to mark Phase 1 complete)
5. `/gsd-discuss-phase 2` — Data Sync phase
</remaining_work>

<decisions_made>

- Atlassian OAuth 2.0 (3LO) over Basic Auth modal — required for future API access + better UX
- `count_projects()` in callback + `/api/auth/me` response determines routing — no extra frontend API call
- Token encrypted with Fernet at rest in SQLite `oauth_tokens` table
- User info fetched from `https://api.atlassian.com/me` (requires `read:me` scope)
- sessionStorage for user display cache — cleared on tab close, re-fetched from `/api/auth/me` on next mount
- OAuth credentials in `backend/.env` (gitignored) — never committed
- `prompt: "consent"` in OAuth params — Atlassian shows account picker on every new auth (enables multi-account switching after logout)
- Logout deletes DB token — next login requires full OAuth flow (by design for account switching)
</decisions_made>

<blockers>

- **[HUMAN ACTION]** Atlassian OAuth app in Development mode — add test email at developer.atlassian.com/console/myapps/ → app → Distribution → Testing
- UAT tests obsolete (test old Basic Auth modal) — must re-scope before running
</blockers>

## Required Reading (in order)

1. `backend/routers/auth.py` — full OAuth flow, `/api/auth/me`, `POST /api/auth/logout`
2. `backend/models/oauth_token.py` — `upsert_oauth_token`, `load_oauth_token`, `delete_oauth_token`
3. `frontend/src/pages/ConnectionPage.jsx` — auth check on mount, spinner, OAuth button, error handling
4. `frontend/src/pages/NoProjectPage.jsx` — user data + nav dropdown with logout
5. `frontend/src/pages/DashboardPage.jsx` — user card + inline logout button

## Infrastructure State

- **Docker**: both containers running (verify with `docker.exe compose ps`)
- **Frontend**: http://localhost:3000 (Vite dev server, HMR polling 500ms)
- **Backend**: http://localhost:8000 (uvicorn --reload)
- **DB**: SQLite at `backend/jira.db` — `oauth_tokens` table holds encrypted token (survives container restarts)
- **Env**: `backend/.env` has real OAuth credentials + FERNET_KEY (gitignored)
- **WSL2**: use `docker.exe` not `docker` (Windows Docker Desktop binary)

## Startup (if containers stopped)

```bash
docker.exe compose up -d --force-recreate
```

<context>

OAuth is fully implemented and credentials are loaded. The only blocker to testing the full login flow is a one-time console setting: the Atlassian OAuth app must have the user's email added as a tester in the Distribution tab. Once that's done, the entire flow (OAuth → callback → token storage → redirect to /no-project) should work without any code changes. Then: re-scope UAT to cover OAuth flow, run it, close Phase 1, start Phase 2 discussion.
</context>

<next_action>

User completes human action: go to https://developer.atlassian.com/console/myapps/ → open OAuth app → Distribution tab → Testing section → add Atlassian email. Then test login at http://localhost:3000. Then re-scope 01-HUMAN-UAT.md and run UAT.
</next_action>
