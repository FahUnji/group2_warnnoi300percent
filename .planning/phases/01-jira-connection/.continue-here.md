---
context: phase
phase: 01-jira-connection
task: 0
total_tasks: 3
status: ready_to_execute
last_updated: 2026-05-11T08:30:00.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

_No blocking constraints identified this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| gsd-sdk not in PATH | `gsd-sdk: command not found` on every SDK call. Entire workflow executed manually by reading workflow files + constructing prompts from first principles. | advisory | Before using any gsd-sdk call, verify with `which gsd-sdk`. If missing, read workflow .md files and execute manually. |

<current_state>
Phase 1 "Jira Connection" fully planned. All 3 PLAN.md files + SKELETON.md committed (commit 37372f3). Plan-checker verified 12/12 dimensions — PASS. Requirements 3/3 (JIRA-01/02/03) and decisions 14/14 (D-01 through D-14) all covered.

Execution has NOT started. Zero tasks executed. This is the transition point from planning → execution.

Note: STATE.md frontmatter says `stopped_at: Phase 1 planned — ready to execute` but the body still shows old "Ready to plan" content — body was not updated. Treat frontmatter as authoritative.
</current_state>

<completed_work>

Planning artifacts (all committed at 37372f3):
- UI-SPEC.md — approved (committed 7c27cf5, revised 7c27cf5 corrected 1901e92)
- PATTERNS.md — 12 new files mapped to skill/ analogs
- PLAN-01.md — Wave 1: FastAPI scaffold + jira_config model + SQL migration (JIRA-01)
- PLAN-02.md — Wave 2: JiraService (Fernet + Jira auth) + REST endpoints (JIRA-02, JIRA-03)
- PLAN-03.md — Wave 3: React frontend, ConnectionForm, StatusBanner, SuccessModal (JIRA-01/02/03)
- SKELETON.md — Walking Skeleton, architectural decisions locked
</completed_work>

<remaining_work>

Execute all 3 plans in wave order:

**Wave 1 — PLAN-01** (autonomous, no deps):
- Task 1: backend/ scaffold (requirements.txt, main.py FastAPI+CORS, database.py mysql-connector, .env.example, __init__ files)
- Task 2: jira_config model (models/jira_config.py upsert/load, migrations/001_create_jira_config.sql, stub routers/jira.py)

**Wave 2 — PLAN-02** (depends on Wave 1):
- Task 1: backend/services/jira_service.py — JiraService with Fernet encrypt/decrypt, _test_jira_auth (requests to /rest/api/3/myself), save_config, verify_saved_credentials
- Task 2: backend/routers/jira.py — POST /api/jira/connect + GET /api/jira/status, Pydantic JiraConfigRequest, D-13 error shape

**Wave 3 — PLAN-03** (depends on Wave 1 + Wave 2):
- Task 1: Vite/React scaffold (package.json, vite.config.js proxy→:8000, App.jsx with auto-verify useEffect, ConnectionPage.jsx, LoadingSpinner, VerifySpinner)
- Task 2: Connection components (ConnectionForm with 3 fields + inline error, StatusBanner 3 variants, SuccessModal with 2s auto-redirect, DashboardPage stub, all CSS Modules)
</remaining_work>

<decisions_made>

Key decisions locked in 01-CONTEXT.md (D-01 through D-14):
- D-01: 3 fields only — base_url, email, api_token
- D-03: Fernet encryption at rest, FERNET_KEY from .env
- D-04: FastAPI (async, Pydantic, auto-docs at /docs)
- D-05: layered structure — routers/ → services/ → models/
- D-07: separate dev servers — React :3000, FastAPI :8000, CORS locked
- D-09: HTML/CSS are visual reference only — build React from scratch
- D-10: success flash → auto-redirect to /dashboard after 2000ms
- D-11: inline error, preserve base_url+email, clear api_token on error
- D-12: 3 error codes — invalid_credentials, unreachable_host, timeout
- D-13: error shape `{"ok": false, "error": "<code>", "message": "<text>"}` HTTP 400
- D-14: auto-verify on app load via GET /api/jira/status
</decisions_made>

<blockers>
None active. Three human pre-checks needed before execution:
1. MySQL running + database accessible (migration 001 needs DB)
2. Node.js + npm installed (Vite scaffold in Wave 3)
3. Python 3.11+ with pip (FastAPI install in Wave 1)
</blockers>

## Required Reading (in order)

1. `.planning/phases/01-jira-connection/01-CONTEXT.md` — all 14 decisions (D-01→D-14), MUST honor every one
2. `.planning/phases/01-jira-connection/01-PLAN-01.md` — Wave 1 tasks, exact file contents
3. `.planning/phases/01-jira-connection/01-PLAN-02.md` — Wave 2 tasks, JiraService spec
4. `.planning/phases/01-jira-connection/01-PLAN-03.md` — Wave 3 tasks, all React components
5. `.planning/phases/01-jira-connection/01-UI-SPEC.md` — design contract (color, spacing, typography, copywriting — all locked)

## Infrastructure State

- Backend: NOT created yet. `backend/` directory does not exist.
- Frontend: NOT created yet. `frontend/` directory does not exist.
- Database: MySQL must be running; `jira_db` (or equivalent) must be creatable/accessible before PLAN-01 execution.
- Dev servers: neither :3000 nor :8000 running yet.
- Uncommitted base files: 32 files in working tree (UI/*, skills/*, planning docs, CLAUDE.md, SPEC.md) were never staged. These are initial project assets — they are NOT in conflict, just unstaged. Commit them first or leave them — they don't block execution.

<context>
Planning complete. All 3 waves planned, verified (12/12 dimensions), and committed. User is ready to execute. They picked "Plan then Execute" and wanted to "start phase 1 and see if it works." Phase 1 wires up the Jira connection end-to-end: credential form → FastAPI endpoint → Jira Basic Auth test → Fernet-encrypted storage in MySQL → auto-verify on load → redirect to dashboard. This is the foundation all other phases (2-5) depend on via the jira_config table.
</context>

<next_action>
Start with: `/gsd-execute-phase 1`

Before running: verify MySQL is up, Python 3.11+ in PATH, Node.js/npm in PATH.

Wave execution order is mandatory: PLAN-01 → PLAN-02 → PLAN-03 (each depends on the previous).
</next_action>
