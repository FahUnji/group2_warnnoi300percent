---
context: phase
phase: 04-sprint-report
task: post-phase-fixes
total_tasks: 20
status: paused
last_updated: 2026-05-13T07:57:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Worktree files committed | `.claude/worktrees/` missing from `.gitignore`; fixed earlier | advisory | `workflow.use_worktrees=false` in config.json; never change |
| npm install EACCES on WSL NTFS | Fails on `/mnt/d/` paths | advisory | Use Docker or native Linux path |
| CRLF diff false positives | `.planning/*.md` show hundreds of modified lines — CRLF only | advisory | Never stage `.planning/` unless content was intentionally edited |
| FastAPI error format | `HTTPException` with dict `detail` serializes as `{"detail": {...}}` not `{"message": ...}` | advisory | Frontend must use `err.detail?.message` not `err.message` |

<current_state>
Phase 04 sprint-report: COMPLETE (d9b4347), but 3 post-phase fixes applied this session, NOT yet committed.

**Uncommitted files:**
1. `backend/services/jira_sprint_service.py` — `_fetch_board_id` returns `"no_board"` error code (was `"api_error"`); `_fetch_sprints_and_store` returns `ok:true, sprints:[]` for `no_board` case instead of propagating HTTP 400
2. `frontend/src/pages/DashboardPage.jsx` — "View Sprints" button added to project card 3-dot dropdown (calendar SVG icon, `cardMenuSync` CSS class); sidebar removed from layout
3. `frontend/src/pages/SprintPage.jsx` — error extraction fixed: `err.detail?.message || err.message || fallback` (FastAPI wraps error in `detail`, not top-level)

**Root cause of HTTP 400 that triggered this session:**
- SAM/MLO projects may not have Jira Agile boards configured
- `_fetch_board_id` returned `{"error": "api_error"}` (too generic) → no cached sprints → router raised 400
- Frontend displayed generic fallback because it read `err.message` (undefined for FastAPI HTTPException)
- Both layers fixed: backend returns empty ok:true; frontend reads correct field

Next: commit the 3 files, UAT in browser, then `/gsd-plan-phase 5`
</current_state>

<completed_work>

- **04-01 Wave 1** (6e9c2a0): sprints table, jira_sprint_service, GET /api/sprints, SprintPage.jsx+css
- **04-02 Wave 2** (933bd72): /sprint route, DashboardPage sidebar Sprint link
- **Hotfix 1** (d4ff9cb): BugReportPage.module.css — added missing .donutTooltip CSS
- **Hotfix 2** (c045b02): Sprint sync: state=active,closed,future; sprint_name JOIN; stale fallback; POST sync fires sprint sync
- **Hotfix 3** (7c683f4): BugReport Refresh Data calls POST /api/sync first; Dashboard Sync Now card menu
- **Hotfix 4** (87ee0f2): SprintPage Refresh Data calls POST /api/sync first; sort active → closed desc → future
- **Code Review** (cf483da): 3 critical, 6 warning, 3 info — hotfix delta since d6b99c6
- **Fix CR-01** (0615edb): isinstance guard + bare except on _fetch_sprints_and_store auth block
- **Fix CR-02** (c59028e): encodeURIComponent on BugReportPage sync + bug fetch calls
- **Fix CR-03** (f1cd4ca): setStale(false) at start of fetchSprints; stale banner decoupled from error
- **Verification** (4643d1e): gaps found (sidebar missing + sprint_name note); fixed same session
- **Restore sidebar** (8205df5): Dashboard sidebar with Sprint link restored — was deleted by aade1e1
- **Phase 04 closed** (d9b4347): STATE.md updated, current focus → Phase 05
- **Post-phase fix A** (uncommitted): jira_sprint_service.py — no_board code + graceful empty return
- **Post-phase fix B** (uncommitted): DashboardPage.jsx — View Sprints in card dropdown
- **Post-phase fix C** (uncommitted): SprintPage.jsx — err.detail?.message fix
</completed_work>

<remaining_work>

- **FIRST**: Commit 3 uncommitted files:
  - `backend/services/jira_sprint_service.py`
  - `frontend/src/pages/DashboardPage.jsx`
  - `frontend/src/pages/SprintPage.jsx`
- **Then**: Browser UAT — reload Sprint page, confirm no HTTP 400, confirm View Sprints navigates correctly, confirm empty state for no-board projects
- **Then**: `/gsd-plan-phase 5` — Export (.xlsx + .docx)
</remaining_work>

<decisions_made>

- Sprint stats JOIN uses sprint_name (not sprint_id): 71% bugs have sprint_id=NULL — intentional
- state=active,closed,future in Jira Board Sprint API: prevents closed sprints from being omitted
- no_board → ok:true + empty list: projects without Agile boards are valid, not an error
- View Sprints in card dropdown: sidebar removed; reuses cardMenuSync CSS (green); calendar icon
- FastAPI HTTPException detail: always use `err.detail?.message` on frontend, not `err.message`
- fetchStats + fetchSprints both call POST /api/sync first
- Export stubs only in Phase 04: real implementation is Phase 05
- use_worktrees=false permanent
</decisions_made>

<blockers>
- npm install EACCES on WSL NTFS — use Docker (pre-existing, non-blocking)
- OAuth token had ~44min remaining at last check — may need reconnect if expired
</blockers>

## Required Reading (in order)
1. `backend/services/jira_sprint_service.py` — sprint sync + no_board handling + stale fallback
2. `frontend/src/pages/SprintPage.jsx` — error extraction pattern + sort + stale banner
3. `frontend/src/pages/DashboardPage.jsx` — View Sprints card dropdown
4. `.planning/ROADMAP.md` — Phase 05 Export requirements (EXPORT-01, EXPORT-02)

## Infrastructure State
- Git branch: `sprint-report-phase-4`
- Working tree: 3 content-changed files uncommitted; `.planning/*.md` CRLF diffs (ignore)
- DB: sprints table populated on first Sprint page load / Refresh Data
- Backend/Frontend: not running — use `docker-compose up`

<context>
Phase 04 formally complete. This session investigated HTTP 400 on Sprint page load. Root: no Agile board for SAM/MLO projects + FastAPI error format mismatch. Fixed both layers. Sidebar was removed in a prior fix; replaced with View Sprints in project card dropdown. Three fixes applied, uncommitted.
</context>

<next_action>
Start with: Commit the 3 post-phase fix files, then UAT in browser, then `/gsd-plan-phase 5`
</next_action>
