---
context: phase
phase: 02-data-sync
task: post-uat-fixup
total_tasks: 1
status: paused
last_updated: 2026-05-12T08:07:10Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Jira pagination — `POST /search/jql` uses `nextPageToken` cursor, not `startAt`. Adding `startAt` → HTTP 400 "Invalid request payload".
- [ ] CONSTRAINT: backend/.env — contains FERNET_KEY + OAuth secrets. Gitignored. Must never be staged or committed.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Jira pagination startAt | `POST /search/jql` returns HTTP 400 if `startAt` in payload. Jira Cloud uses cursor pagination only. Hard cap 100/page. | blocking | Never add startAt to POST /search/jql; always use nextPageToken loop |
| backend/.env commit | `.env` contains FERNET_KEY + OAuth secrets — never staged | blocking | Always `git add` files by explicit name; never `git add .` or `git add -A` |
| JQL f-string interpolation | `project_key` interpolated into JQL string (not SQL). SQL uses `?` params. Accepted per threat model T-02-04. | advisory | Do not expand to other fields; SQL must stay parameterized |

<current_state>
Phase 02 (data-sync) is COMPLETE — human UAT 4/4 passed.

Post-UAT session: user decided dashboard shows only Bug and Bug Task issues (459 total in SAM project). Subtask fetch (Step 2) removed from _fetch_bugs(). One uncommitted file: backend/services/jira_sync_service.py.

Phase 03 (Dashboard UI — charts + project switcher) is ready to plan.
</current_state>

<completed_work>

- Phase 02 plans 01 and 02: fully executed and committed (commits 56d4124, b3325d4)
- Phase 02 human UAT 4/4 passed (commit 3945e14)
- Pagination fix: switched POST /search/jql from broken startAt offset → nextPageToken cursor (committed b3325d4)
- Scope simplification this session: _fetch_bugs() reduced to single-step JQL — issuetype in (Bug, "Bug Task") only. UNCOMMITTED.
</completed_work>

<remaining_work>

1. Commit backend/services/jira_sync_service.py (subtask removal)
2. /gsd-plan-phase 3 — Dashboard UI
</remaining_work>

<decisions_made>

- Dashboard shows Bug and Bug Task issues only — no subtasks. User decision 2026-05-12. 459 issues for SAM project.
- Single-step JQL: `issuetype in (Bug, "Bug Task") ORDER BY created DESC` — no two-step fetch, no dedup needed.
- Jira pagination: nextPageToken cursor loop, _PAGE_SIZE=100 (Jira Cloud hard cap).
- GET /api/bugs/{project_key} exists as separate read endpoint — reads SQLite cache, does not re-sync.
</decisions_made>

<blockers>

None blocking. Optional: re-sync from UI to confirm 459 issues stored (old DB may still have 509 from previous two-step run).
</blockers>

## Required Reading (in order)

1. `.planning/phases/02-data-sync/02-VERIFICATION.md` — phase 02 verification + what was built
2. `.planning/ROADMAP.md` Phase 3 section — goal, success criteria, requirements for Dashboard UI

## Infrastructure State

- Docker: `docker compose up` from project root; backend port 8000, frontend port 3000
- Backend hot-reload: uvicorn --reload; jira_sync_service.py change already live after save
- SQLite DB: `backend/jira_bug_summary.db` — may still have 509 rows (old two-step count); re-sync gives 459
- OAuth: Phase 1 OAuth token must be in DB for sync to work

<context>
Phase 02 complete. Post-UAT the user clarified scope: dashboard should show only the 459 Bug/Bug Task issues, not the 64 subtasks (child tickets). _fetch_bugs() simplified to single JQL query. One commit pending then Phase 03 planning.
</context>

<next_action>
Start with: `git add backend/services/jira_sync_service.py` and commit "fix(sync): remove subtask fetch — dashboard shows Bug/Bug Task only". Then /gsd-plan-phase 3.
</next_action>
