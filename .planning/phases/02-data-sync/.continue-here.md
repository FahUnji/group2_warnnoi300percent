---
context: phase
phase: 02-data-sync
task: human-uat
total_tasks: 4
status: in_progress
last_updated: 2026-05-12T07:02:08Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

_No blocking constraints identified this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| JQL f-string interpolation | `project_key` interpolated into JQL string (not SQL — SQL uses `?` params). Jira enforces project access. Accepted per threat model T-02-04. | advisory | Do not expand to other fields; SQL must stay parameterized |

<current_state>
Phase 02 (data-sync) is code-complete and automated verification passed (8/8). 4 human UAT tests remain.

Session also added enhancements beyond original phase scope:
1. NoProjectPage: search box UX — list only shows when user types (replaces always-visible scrollable list)
2. JQL expanded to include Bug subtasks: `issuetype = Bug OR (issueType in subTaskIssueTypes() AND parent in (...))`
3. New endpoint GET /api/bugs/{project_key} — fetches stored bugs from SQLite
4. DashboardPage: full styled bug table (Key, Summary, Status, Priority, Sprint, Assignee) with colored badges

These changes are UNCOMMITTED. backend/.env is also modified but must NOT be committed (contains secrets).
</current_state>

<completed_work>

Phase 02 automated verification — all 8 must-haves VERIFIED (see 02-VERIFICATION.md)

Session additions (all uncommitted):
- Task A: NoProjectPage search-box UX (NoProjectPage.jsx + NoProjectPage.module.css)
- Task B: JQL subtask expansion (jira_sync_service.py line 51)
- Task C: GET /api/bugs/{project_key} endpoint (sync.py)
- Task D: DashboardPage bug table with status/priority badges (DashboardPage.jsx)
</completed_work>

<remaining_work>

1. Commit all uncommitted code changes (skip backend/.env)
2. Human UAT — 4 browser tests (see 02-HUMAN-UAT.md):
   - UAT-1: NoProjectPage renders real Jira project list from /api/projects
   - UAT-2: Project click → sync status bar → navigate to /dashboard
   - UAT-3: Sync Now button → spinner → lastSynced timestamp updates
   - UAT-4: Direct /dashboard nav without project → warning banner
3. After UAT passes: mark Phase 2 complete, update STATE.md, plan Phase 3
</remaining_work>

<decisions_made>

- Show project list only when search query non-empty (hide by default) — reduces visual noise on load
- Include Bug subtasks in JQL using subTaskIssueTypes() with parent constraint — avoids fetching all subtasks
- Added GET /api/bugs/{project_key} as a read endpoint separate from sync — dashboard can load cached data without re-syncing
- Bug table in DashboardPage uses inline styles (consistent with existing DashboardPage pattern, no CSS module)
</decisions_made>

<blockers>

- Human UAT requires live Jira OAuth token in DB (from Phase 1 OAuth flow) + running Docker containers
- backend/.env must not be committed — contains FERNET_KEY and other secrets
</blockers>

## Required Reading (in order)

1. `.planning/phases/02-data-sync/02-VERIFICATION.md` — automated verification results + human UAT items
2. `.planning/phases/02-data-sync/02-HUMAN-UAT.md` — 4 UAT tests with exact steps

## Infrastructure State

- Docker: `docker compose up` from project root; backend port 8000, frontend port 3000
- Backend hot-reload: uvicorn --reload watches `backend/`
- Frontend hot-reload: Vite polls every 100ms (WSL2 inotify workaround)
- SQLite DB: `backend/jira_bug_summary.db` (auto-created on startup)
- OAuth: Phase 1 must have completed successfully to have a valid token in DB

<context>
Phase 2 code is complete and verified. Extra enhancements (search UX, subtask JQL, bug table) were done this session at user request. All changes hot-reload without container restart. Human UAT is the final gate before Phase 3 (Dashboard UI — charts + project switcher).
</context>

<next_action>
Start with: `git add backend/routers/sync.py backend/services/jira_sync_service.py frontend/src/pages/DashboardPage.jsx frontend/src/pages/NoProjectPage.jsx frontend/src/pages/NoProjectPage.module.css` and commit as "feat(02): search UX, subtask JQL, bug table display". Then run human UAT.
</next_action>
