---
context: phase
phase: 05-export
task: null
total_tasks: 0
status: paused
last_updated: 2026-05-13T12:56:11Z
---

# Post-Phase UI Polish — Session 2

_No blocking constraints._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| uvicorn watcher blind to NTFS | `--reload` does NOT detect .py changes on WSL NTFS mount | advisory | Always `docker restart group2_warnnoi300percent-backend-1` after editing .py files |
| Docker CLI not in WSL | `docker` command not found in WSL shell | advisory | Use: `powershell.exe -Command "cd '...'; docker ..."` |
| CRLF diff false positives | `.planning/*.md` show hundreds of modified lines — CRLF only | advisory | Never stage `.planning/` unless content was intentionally edited |
| Excel sheet title invalid chars | openpyxl raises ValueError on `/ \ * ? : [ ]` in sheet name | advisory | Always sanitize: `re.sub(r'[\\/*?:\[\]]', '-', name)[:31]` |
| Flex truncation needs min-width:0 | text-overflow:ellipsis won't work on flex child without min-width:0 on container | advisory | Always add `min-width:0; flex:1` to text container inside flex row |

<current_state>
Post-phase UI polish session 2 complete. Backend needs restart — sync.py + export.py modified.
All 5 phases remain complete. Changes uncommitted.
Branch: export-phase-5
</current_state>

<completed_work>

This session (UI polish #2):
- GET /api/projects/{project_key} endpoint — returns project_name + site_name from DB
- BugReportPage sidebar: project_key (short) on top, projectFullName (full name) below
- SprintPage sidebar: project_key (short) on top, projectName (full name) below
- Sidebar sizing: BugReportPage matched to SprintPage as base (256px, #c3c6d6 border, gap:14px, 16px font)
- Both sidebars: text-overflow:ellipsis on projectName + projectSub (single line truncation)
- export.py bugs xlsx/docx: added project_name row/paragraph below project_key (conditional — only if non-empty)
- Dashboard cards: min-height 160→200px, statVal 26→32px, statBox padding enlarged, cardContent padding/gap enlarged
- Dashboard addDesc: kept base sentence, added detail copy matching html mockup style

Previous session (UI polish #1, committed):
- `c9d2c97`: wip pause — export polish complete
- `90e984d`: fix sprint state 'released' → 'Completed'
- `1f203ac`: bug report xlsx summary stats block
- `a7b1825`: sprint xlsx metadata/stats/date per sheet
- `ed126d8`: sprint xlsx No Sprint + unknown sprint sheets
</completed_work>

<remaining_work>
- Restart backend (REQUIRED before testing new endpoint)
- Manual UAT: sidebar project names on BugReportPage + SprintPage
- Manual UAT: dashboard card size + addDesc text
- Optional: `/gsd-audit-milestone`, `/gsd-code-review`
</remaining_work>

<decisions_made>

- Sidebar: project_key short on top, full project_name below — NOT site_name (user: "Manao Meal not Manao Software")
- BugReportPage fetches from new GET /api/projects/{project_key}; SprintPage already had projectName from /api/projects
- Truncation: projectInfo container needs min-width:0 + flex:1 for ellipsis to work in flex row
- Sprint sidebar is the sizing base (user specified)
- export.py: project_name conditional — only added if non-empty string
</decisions_made>

<blockers>
- Backend restart required: sync.py + export.py modified, uvicorn blind to NTFS changes
- `backend/services/jira_sprint_service.py` shows as modified — pre-existing change from before this session, not touched this session
</blockers>

## Infrastructure State
- Branch: `export-phase-5`
- Backend: port 8000 — **NEEDS RESTART** after this session's .py changes
- Frontend: port 3000 (Vite HMR already applied CSS/JSX changes)
- Access: http://localhost:3000 | http://localhost:8000/docs

<context>
UI polish session. Fixed sidebar project name display (short key + full name), made sidebars consistent size, added ellipsis truncation, made dashboard cards bigger, updated addDesc copy. All changes clean and minimal — no regressions expected.
</context>

<next_action>
Restart backend: `powershell.exe -Command "docker restart group2_warnnoi300percent-backend-1"`. Then UAT sidebar + dashboard in browser.
</next_action>
